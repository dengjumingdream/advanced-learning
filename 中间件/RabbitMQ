三、信息存储方式
    1.消息和数据都存储在内存中
    2.消息和数据存储在磁盘和内存中
    3.都存在磁盘中

四、AMQP三层协议
    1.Module Layer：协议最高层，定义了一些客户端调用的命令
    2.Session Layer：主要负责客户端命令发送到服务器，再将服务端应答客户端，提供可靠性同步机制和错误处理
    3.Transport Layer：最底层，主要传输二进制数据流，提供帧的处理、信道复用，错误检测和数据表示

五、AMQP三大组件
    1.交换器（Exchange）：消息代理服务中用于把消息路由到队列的组件
    2.队列（Queue）：用来存储消息的数据结构，位于磁盘或者内存中
    3.绑定（Bing）：一套规则，告知交换器消息应该把消息投递到哪个队列中

七、交换器类型

八、Rabbit名词
    1.ConnectionFactory（连接管理器）：应用程序和Rabbit之间建立连接的管理器，代码中使用
    2.Channel（管道）：消息推送的管道
    3.Exchange（交换机）：用于接收、分配消息
    4.Queue（队列）：用于存储生产者消息
    5.RoutingKey（路由键）：用于将生产者的数据分配到交换机上；
    6.Binging（绑定键）：用于将交换机的消息绑定到队列上；

    用户 -ConnectionFactory-> RabbitMq内部( -RoutingKey-> Exchange -BingdingKey-> (Queue1、Queue2))

九、队列类型
    1.生产者，消费者，一个队列，一对一
    2.生产者，消费者，一个队列，一对多
    3.Fanout Exchange
      - 1、1个生产者，多个消费者
      - 2、每一个消费者都有自己的一个队列
      - 3、生产者没有将消息直接发送到队列，而是发送到了交换机
      - 4、每个队列都要绑定到交换机
      - 5、生产者发送的消息，经过交换机，到达队列，实现，一个消息被多个消费者获取的目的
      - 注意：一个消费者队列可以有多个消费者实例，只有其中一个消费者实例会消费
    4.路由模式：通过交换机匹配对应队列
    5.主题模式 Topic Change(通配符)